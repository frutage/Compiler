struct fries {
  int number;
  float flavor;
};

int tasty(){
  struct fries Mc;
  Mc.number = 30;
  Mc.flavor = -1.2;
  while (Mc.number > 13) {
    Mc.flavor = Mc.flavor + 1.2;
    Mc.number =Mc.number - 3;
  }

  if (Mc.number < 24) {
    return Mc.number;
  } else {
    return Mc.flavor;
  }
}
